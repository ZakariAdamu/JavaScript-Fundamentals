/*========= Math.random() ======== */

// Generate a decimal number between 0 and 0.99:
// Math.random();

// Save it to a variable:
// var decimal = Math.random();

// Log the value of decimal to the console
// console.log(decimal)

// Log the value of decimal MULTIPLIED BY 10
// console.log(decimal * 10);

/*========= Math.ceil() ======== */
// Round up any decimal number to the nearest integer
// var roundUp = Math.ceil(0.0001)

// console.log(roundUp)

var decimalValue = Math.random() * 10;

console.log(decimalValue);

var integer = Math.ceil(decimalValue)

console.log(integer)
